# Local E2E Testing Environment Configuration
# Copy this file to .env.local and configure for local e2e testing

# Server Configuration
# Using non-standard ports to avoid conflicts with other services
PORT=8042
BASE_URL=http://localhost:8042

# Firestore Emulator Configuration
# Note: local-e2e-server.sh will use these values when starting the emulator
FIRESTORE_EMULATOR_PORT=8092
FIRESTORE_EMULATOR_HOST=localhost:8092
GOOGLE_CLOUD_PROJECT=test-project

# JWT Configuration (use test values for local e2e)
JWT_SECRET_KEY=test-secret-key-for-local-e2e-testing-only-do-not-use-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Garmin OAuth (use test credentials or mock in e2e tests)
GARMIN_CONSUMER_KEY=test-key
GARMIN_CONSUMER_SECRET=test-secret

# OpenAI (use test key or mock in e2e tests)
OPENAI_API_KEY=test-key

# Encryption (use test key for local e2e)
ENCRYPTION_KEY=test-encryption-key-32-bytes-1234

# Environment
ENVIRONMENT=local-e2e

# Note: This configuration is for LOCAL E2E TESTING ONLY
# Real credentials are stored in GCP Secret Manager
# Never commit real secrets to this file
