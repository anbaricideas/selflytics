# direnv configuration for Selflytics project
# This ensures the correct gcloud configuration is active when working in this directory

export CLOUDSDK_ACTIVE_CONFIG_NAME=selflytics

# Optional: Add other project-specific environment variables here
# export GCP_PROJECT_ID=selflytics-infra
# export GCP_REGION=australia-southeast1
