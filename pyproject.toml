[project]
name = "selflytics"
version = "0.1.0"
description = "AI-powered analysis for quantified self data"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["spike"]

[dependency-groups]
dev = []
